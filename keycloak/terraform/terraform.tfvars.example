# Keycloak connection
keycloak_url            = "http://localhost:8080"
keycloak_admin_username = "admin"
keycloak_admin_password = "changeme"

# Realm
realm_name         = "rmu"
realm_display_name = "RMU Platform"

# Client
rmu_client_secret = "changeme"

rmu_client_valid_redirect_uris = [
  "http://localhost:*/*",
  "https://app.example.com/*",
]

rmu_client_web_origins = [
  "http://localhost:*",
  "https://app.example.com",
]

# Users
primary_user_username   = "johndoe"
primary_user_email      = "user@example.com"
primary_user_first_name = "John"
primary_user_last_name  = "Doe"
primary_user_password   = "changeme"
